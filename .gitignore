# Build directories
build/
build_asan/
build-asan/
build-android/
build-android-deps/
build-debug/
build-sanitize/
bin/
lib/

# ctest writes its transcript here when run from the repo root.
Testing/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
!CMakeLists.txt
# Hand-written CMake modules live in cmake/ and are source, not build output.
!cmake/*.cmake

# Compiled Object files
*.o
*.obj
*.slo
*.lo
*.spv
!assets/shaders/footprint.vert.spv
!assets/shaders/footprint.frag.spv

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Compiled Static libraries
*.a
*.lib
*.la

# Executables
*.exe
*.out
*.app
/wowee

# Claude Code internal state
.claude/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Compilation database (regenerated by cmake)
compile_commands.json

# Language server caches
.ccls
.ccls-cache/
.cache/clangd/

# Tags
tags
TAGS
.tags
cscope.out

# External dependencies (except submodules and vendored headers)
extern/*
!extern/.gitkeep
!extern/catch2
!extern/imgui
!extern/vk-bootstrap
!extern/FidelityFX-SDK
!extern/FidelityFX-FSR2
!extern/vk_mem_alloc.h
!extern/lua-5.1.5
!extern/VERSIONS.md

# ImGui state
imgui.ini

# Logs
*.log

# Config files
config.ini
config.json
.env

# Local signing and notarization handoff material
.secrets/

# Runtime cache (floor heights, etc.)
cache/

# Single-player saves
saves/
wowee_[0-9][0-9][0-9][0-9]

# Extracted assets (run ./extract_assets.sh or .\extract_assets.ps1 to generate)
Data/*
!Data/opcodes

# A profile is configuration, not an asset: the four JSONs an expansion is
# defined by are tracked, and everything else that lands in its directory is
# extracted and is not. The four existing profiles predate the Data/* rule and
# stayed tracked by being tracked already; a fifth needs saying.
!Data/expansions/
Data/expansions/*
!Data/expansions/*/
Data/expansions/*/*
!Data/expansions/*/expansion.json
!Data/expansions/*/opcodes.json
!Data/expansions/*/update_fields.json
!Data/expansions/*/dbc_layouts.json
!Data/expansions/*/movement_sequences.json

ingest/

# Asset archives dropped at the repo root rather than into ingest/. An MPQ is
# hundreds of megabytes and `git add -A` will take one without complaint.
*.MPQ
*.mpq

# Asset pipeline state and texture packs
asset_pipeline/

# Local texture dumps / extracted art should never be committed
assets/textures/
node_modules/

# Python cache artifacts
tools/__pycache__/
*.pyc

# World editor output: scaffolded/exported zones and per-user ImGui layout
custom_zones/
wowee_editor_layout.ini

#agents
AGENTS.md
codex-loop.sh

# The headless runner keeps its config here so it cannot overwrite the
# player's missing-API and Lua-error lists. One root per check that drives it,
# so a run cannot read a store some other check left: the runner loads the CVar
# store at start-up, and a shared root made a result depend on run order.
logs/*_config/

# Canary builds: deliberately broken binaries for watching a check fail.
# Kept out of build/ because that is what the player runs.
build-canary/

# Android Gradle build outputs
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/local.properties

# Addon SavedVariables, which the client writes next to the addon it saves
# them for - including the bundled ones in addons/.
*.lua.saved

# Asset packs built from a client you own - tools/asset_pack_from_client.py
# writes here. The art in them is Blizzard's and comes out of your own
# installation; it does not belong in this repository. See
# docs/upgraded-assets.md.
asset_packs/
