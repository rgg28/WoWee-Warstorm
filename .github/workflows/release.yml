name: Release

on:
  push:
    tags: [ 'v*' ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  build-linux:
    name: Build (linux-${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
        - arch: x86-64
          runner: ubuntu-24.04
        - arch: arm64
          runner: ubuntu-24.04-arm

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Cache apt packages
      uses: actions/cache@v4
      with:
        path: /var/cache/apt/archives/*.deb
        key: apt-${{ matrix.arch }}-${{ hashFiles('.github/workflows/release.yml') }}
        restore-keys: apt-${{ matrix.arch }}-

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          cmake \
          build-essential \
          pkg-config \
          libglm-dev \
          libssl-dev \
          zlib1g-dev \
          libvulkan-dev \
          vulkan-tools \
          glslc \
          libavformat-dev \
          libavcodec-dev \
          libswscale-dev \
          libavutil-dev \
          libunicorn-dev \
          libxext-dev \
          libxrandr-dev \
          libxcursor-dev \
          libxi-dev \
          libxfixes-dev \
          libxss-dev \
          libxkbcommon-dev \
          libwayland-dev \
          wayland-protocols \
          libdecor-0-dev \
          libx11-dev
        sudo apt-get install -y libstorm-dev || true

    - name: Build SDL3 from source
      run: |
        # Ubuntu 24.04 has no libsdl3-dev: SDL3 landed in 25.04. Built here
        # the way StormLib already is, rather than pinning the job to a newer
        # runner image for one package.
        set -euo pipefail
        git clone --depth 1 --branch release-3.2.24 \
          https://github.com/libsdl-org/SDL.git /tmp/SDL3
        cmake -S /tmp/SDL3 -B /tmp/SDL3/build \
          -DCMAKE_BUILD_TYPE=Release \
          -DSDL_SHARED=ON -DSDL_STATIC=OFF -DSDL_TEST_LIBRARY=OFF
        cmake --build /tmp/SDL3/build --parallel "$(nproc)"
        sudo cmake --install /tmp/SDL3/build
        sudo ldconfig

    - name: Configure
      run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

    - name: Verify Release Config
      run: |
        cmake -LA -N build | grep -E '^CMAKE_BUILD_TYPE:STRING=Release$'

    - name: Build
      run: cmake --build build --parallel "$(nproc)"

    - name: Package
      run: |
        TAG="${GITHUB_REF_NAME}"
        STAGING="wowee-${TAG}-linux-${{ matrix.arch }}"
        mkdir -p "${STAGING}"

        # Binary
        cp build/bin/wowee "${STAGING}/"

        # Required, not copied if present: see the Windows job for what a
        # soft check shipped.
        test -x build/bin/asset_extract
        cp build/bin/asset_extract "${STAGING}/"

        # The asset manager window, and the script for the command line.
        test -x build/bin/wowee_assets
        cp build/bin/wowee_assets "${STAGING}/"
        cp extract_assets.sh "${STAGING}/"

        # This client's own addons, which the build puts beside the binary and
        # every release so far has left behind. WoweeAllBags is the combined bag
        # window and the Sort Bags button - the two things the client used to
        # draw itself and handed to FrameXML, which has neither - so on a
        # released build they simply were not there.
        cp -r addons "${STAGING}/"

        # Assets (exclude proprietary music)
        rsync -a --exclude='Original Music' build/bin/assets/ "${STAGING}/assets/"

        # Data directory (git-tracked files only)
        git ls-files Data/ | while read -r f; do
          mkdir -p "${STAGING}/$(dirname "$f")"
          cp "$f" "${STAGING}/$f"
        done

        tar czf "${STAGING}.tar.gz" "${STAGING}"

    - name: Upload artifact
      uses: actions/upload-artifact@v5
      with:
        name: wowee-linux-${{ matrix.arch }}
        path: wowee-*.tar.gz
        if-no-files-found: error

  build-macos:
    name: Build (macOS ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      # Each architecture builds natively on its own runner, the way Linux and
      # Windows do here. Not a universal binary: every dependency would have to
      # be universal too, and Homebrew's are per-arch.
      fail-fast: false
      matrix:
        include:
        - arch: arm64
          runner: macos-15
          verify_arch: arm64
        - arch: x86-64
          # macos-13 was the old Intel image and has been retired; a job asking
          # for it queues for ever rather than failing. macos-15-intel is the
          # current x64 label.
          runner: macos-15-intel
          # What lipo calls it, which is not what the archive is named.
          verify_arch: x86_64

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Install dependencies
      run: |
        brew install cmake pkg-config sdl3 glm openssl@3 zlib ffmpeg unicorn \
          stormlib vulkan-loader vulkan-headers molten-vk shaderc

    - name: Configure
      run: |
        BREW=$(brew --prefix)
        PKG_CONFIG_PATH="$BREW/lib/pkgconfig:$(brew --prefix ffmpeg)/lib/pkgconfig:$(brew --prefix openssl@3)/lib/pkgconfig:$(brew --prefix vulkan-loader)/lib/pkgconfig:$(brew --prefix shaderc)/lib/pkgconfig"
        export PKG_CONFIG_PATH
        cmake -S . -B build \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0 \
          -DCMAKE_PREFIX_PATH="$BREW" \
          -DOPENSSL_ROOT_DIR="$(brew --prefix openssl@3)"

    - name: Verify Release Config
      run: |
        cmake -LA -N build | grep -E '^CMAKE_BUILD_TYPE:STRING=Release$'

    - name: Build
      run: cmake --build build --parallel "$(sysctl -n hw.logicalcpu)"

    - name: Configure Developer ID signing
      env:
        APPLE_CERT_P12: ${{ secrets.APPLE_DEVELOPER_ID_CERT_P12_BASE64 }}
        APPLE_CERT_PASSWORD: ${{ secrets.APPLE_DEVELOPER_ID_CERT_PASSWORD }}
        APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_DEVELOPER_ID_IDENTITY }}
      run: |
        if [ -z "${APPLE_CERT_P12}" ] || [ -z "${APPLE_CERT_PASSWORD}" ] || \
           [ -z "${APPLE_SIGNING_IDENTITY}" ]; then
          echo "::error::Apple Developer ID secrets are required for macOS releases."
          exit 1
        fi

        KEYCHAIN_PATH="${RUNNER_TEMP}/wowee-signing.keychain-db"
        KEYCHAIN_PASSWORD="$(openssl rand -base64 32)"
        printf '%s' "${APPLE_CERT_P12}" | base64 -D \
          > "${RUNNER_TEMP}/developer-id.p12"
        security create-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
        security set-keychain-settings -lut 21600 "${KEYCHAIN_PATH}"
        security unlock-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
        security import "${RUNNER_TEMP}/developer-id.p12" \
          -k "${KEYCHAIN_PATH}" -P "${APPLE_CERT_PASSWORD}" -A -t cert -f pkcs12
        security set-key-partition-list -S apple-tool:,apple: -s \
          -k "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
        security list-keychain -d user -s "${KEYCHAIN_PATH}"
        echo "MACOS_SIGNING_ENABLED=true" >> "${GITHUB_ENV}"
        echo "APPLE_SIGNING_IDENTITY=${APPLE_SIGNING_IDENTITY}" >> "${GITHUB_ENV}"

    - name: Create .app bundle
      run: |
        TAG="${GITHUB_REF_NAME}"
        VERSION="${TAG#v}"
        VERSION="${VERSION%%-*}"
        mkdir -p Wowee.app/Contents/{MacOS,Frameworks,Resources}
        bash tools/macos/create_icns.sh assets/Wowee.png \
          Wowee.app/Contents/Resources/Wowee.icns

        # Actual binary
        cp build/bin/wowee Wowee.app/Contents/MacOS/wowee_bin

        # Asset extraction is a required part of the macOS distribution.
        test -x build/bin/asset_extract
        cp build/bin/asset_extract Wowee.app/Contents/MacOS/

        # The asset manager window, beside the binaries rather than in
        # Resources: it is a program, not a file the app reads.
        test -x build/bin/wowee_assets
        cp build/bin/wowee_assets Wowee.app/Contents/MacOS/

        cp extract_assets.sh Wowee.app/Contents/Resources/

        # This client's own addons. Into Resources rather than MacOS, because
        # that is the directory the app makes current at startup and the addon
        # search is relative to it.
        cp -r addons Wowee.app/Contents/Resources/

        # Assets (exclude proprietary music)
        rsync -a --exclude='Original Music' build/bin/assets/ \
          Wowee.app/Contents/Resources/assets/

        # Data directory (git-tracked files only)
        git ls-files Data/ | while read -r f; do
          mkdir -p "Wowee.app/Contents/Resources/$(dirname "$f")"
          cp "$f" "Wowee.app/Contents/Resources/$f"
        done

        # The Vulkan loader is not a graphics driver. Bundle MoltenVK and its
        # ICD manifest so the app works on a clean Mac without Homebrew or the
        # LunarG SDK installed.
        MOLTENVK_PREFIX="$(brew --prefix molten-vk)"
        cp -L "${MOLTENVK_PREFIX}/lib/libMoltenVK.dylib" \
          Wowee.app/Contents/Frameworks/libMoltenVK.dylib

        # Copied explicitly although the client now links SDL3 directly and
        # the Mach-O walker below would find it: the walker rewrites what it
        # finds, and having the dylib already in place keeps that one step.
        SDL3_PREFIX="$(brew --prefix sdl3)"
        cp -L "${SDL3_PREFIX}/lib/libSDL3.dylib" \
          Wowee.app/Contents/Frameworks/libSDL3.dylib
        mkdir -p Wowee.app/Contents/Resources/vulkan/icd.d
        python3 - \
          "${MOLTENVK_PREFIX}/etc/vulkan/icd.d/MoltenVK_icd.json" \
          Wowee.app/Contents/Resources/vulkan/icd.d/MoltenVK_icd.json <<'PY'
        import json
        import pathlib
        import sys

        source, destination = map(pathlib.Path, sys.argv[1:])
        manifest = json.loads(source.read_text(encoding="utf-8"))
        manifest["ICD"]["library_path"] = "../../../Frameworks/libMoltenVK.dylib"
        manifest["ICD"]["is_portability_driver"] = True
        destination.write_text(json.dumps(manifest, indent=2) + "\n", encoding="utf-8")
        PY

        # Traverse every non-system dependency from the original Mach-O files,
        # copy only the referenced install names, and rewrite the whole graph.
        BUNDLE_ROOTS=(Wowee.app/Contents/MacOS/wowee_bin)
        if [ -f Wowee.app/Contents/MacOS/asset_extract ]; then
          BUNDLE_ROOTS+=(Wowee.app/Contents/MacOS/asset_extract)
        fi
        if [ -f Wowee.app/Contents/MacOS/wowee_assets ]; then
          BUNDLE_ROOTS+=(Wowee.app/Contents/MacOS/wowee_assets)
        fi
        BUNDLE_ROOTS+=(Wowee.app/Contents/Frameworks/libMoltenVK.dylib)
        BUNDLE_ROOTS+=(Wowee.app/Contents/Frameworks/libSDL3.dylib)
        python3 tools/macos/bundle_dependencies.py \
          --app Wowee.app \
          --search-dir "$(brew --prefix sdl3)/lib" \
          --search-dir "$(brew --prefix vulkan-loader)/lib" \
          --search-dir "$(brew --prefix openssl@3)/lib" \
          --search-dir "$(brew --prefix ffmpeg)/lib" \
          --search-dir "$(brew --prefix unicorn)/lib" \
          --search-dir "$(brew --prefix stormlib)/lib" \
          --search-dir "$(brew --prefix glm)/lib" \
          "${BUNDLE_ROOTS[@]}"

        # Info.plist
        cat > Wowee.app/Contents/Info.plist << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
          "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0"><dict>
          <key>CFBundleExecutable</key><string>wowee_bin</string>
          <key>CFBundleIdentifier</key><string>com.wowee.app</string>
          <key>CFBundleName</key><string>Wowee</string>
          <key>CFBundleIconFile</key><string>Wowee</string>
          <key>CFBundleVersion</key><string>${GITHUB_RUN_NUMBER}</string>
          <key>CFBundleShortVersionString</key><string>${VERSION}</string>
          <key>CFBundlePackageType</key><string>APPL</string>
          <key>LSMinimumSystemVersion</key><string>13.0</string>
        </dict></plist>
        EOF

        # Build a visible, independently signed extraction launcher.
        bash tools/macos/create_asset_extractor_app.sh \
          "Wowee Asset Extractor.app" "${VERSION}" \
          Wowee.app/Contents/Resources/Wowee.icns

        IDENTITY="-"
        if [ "${MACOS_SIGNING_ENABLED}" = "true" ]; then
          IDENTITY="${APPLE_SIGNING_IDENTITY}"
        fi
        bash tools/macos/sign_app.sh Wowee.app "${IDENTITY}" \
          tools/macos/wowee.entitlements
        bash tools/macos/sign_app.sh "Wowee Asset Extractor.app" "${IDENTITY}"
        bash tools/macos/verify_signature.sh Wowee.app "${IDENTITY}" \
          com.apple.security.cs.allow-jit
        bash tools/macos/verify_signature.sh \
          "Wowee Asset Extractor.app" "${IDENTITY}"

    - name: Verify bundled dylibs
      run: |
        bash tools/macos/verify_bundle.sh Wowee.app ${{ matrix.verify_arch }}
        codesign --verify --deep --strict --verbose=2 "Wowee Asset Extractor.app"

    - name: Create DMG
      run: |
        set -euo pipefail
        TAG="${GITHUB_REF_NAME}"
        DMG="wowee-${TAG}-macos-${{ matrix.arch }}.dmg"
        rm -f "${DMG}"
        rm -rf dmg-root
        mkdir dmg-root
        mv Wowee.app dmg-root/
        mv "Wowee Asset Extractor.app" dmg-root/
        cp tools/macos/ASSET_EXTRACTION_README.txt \
          "dmg-root/README - Asset Extraction.txt"
        ln -s /Applications dmg-root/Applications
        # A runner can leave a Wowee volume mounted from an earlier job, and
        # hdiutil will not create over it. build.yml hit this and grew both a
        # detach and a retry; this step is the one that cannot be re-run without
        # cutting the tag again, and it had neither.
        if [ -d "/Volumes/Wowee" ]; then
          hdiutil detach "/Volumes/Wowee" -force || true
          sleep 2
        fi

        ok=0
        for attempt in 1 2 3 4 5; do
          if hdiutil create -volname Wowee -srcfolder dmg-root -ov -format UDZO "${DMG}"; then
            ok=1
            break
          fi
          echo "hdiutil create failed on attempt ${attempt}; retrying..."
          if [ -d "/Volumes/Wowee" ]; then
            hdiutil detach "/Volumes/Wowee" -force || true
          fi
          sleep 3
        done

        if [ "$ok" -ne 1 ] || [ ! -f "${DMG}" ]; then
          echo "Failed to create ${DMG} after retries."
          exit 1
        fi

        codesign --force --sign "${APPLE_SIGNING_IDENTITY}" \
          --timestamp "${DMG}"
        codesign --verify --strict --verbose=2 "${DMG}"

    - name: Notarize and staple DMG
      env:
        APPLE_NOTARY_KEY: ${{ secrets.APPLE_NOTARY_KEY_P8_BASE64 }}
        APPLE_NOTARY_KEY_ID: ${{ secrets.APPLE_NOTARY_KEY_ID }}
        APPLE_NOTARY_ISSUER_ID: ${{ secrets.APPLE_NOTARY_ISSUER_ID }}
      run: |
        if [ "${MACOS_SIGNING_ENABLED}" != "true" ] || \
           [ -z "${APPLE_NOTARY_KEY}" ] || [ -z "${APPLE_NOTARY_KEY_ID}" ] || \
           [ -z "${APPLE_NOTARY_ISSUER_ID}" ]; then
          echo "::error::Notarization credentials are required for macOS releases."
          exit 1
        fi

        DMG="wowee-${GITHUB_REF_NAME}-macos-${{ matrix.arch }}.dmg"
        printf '%s' "${APPLE_NOTARY_KEY}" | base64 -D \
          > "${RUNNER_TEMP}/AuthKey.p8"
        xcrun notarytool submit "${DMG}" \
          --key "${RUNNER_TEMP}/AuthKey.p8" \
          --key-id "${APPLE_NOTARY_KEY_ID}" \
          --issuer "${APPLE_NOTARY_ISSUER_ID}" \
          --wait --timeout 30m
        xcrun stapler staple "${DMG}"
        xcrun stapler validate "${DMG}"
        spctl --assess --type open --context context:primary-signature \
          --verbose=2 "${DMG}"

    - name: Upload artifact
      uses: actions/upload-artifact@v5
      with:
        name: wowee-macos-${{ matrix.arch }}
        path: wowee-*.dmg
        if-no-files-found: error

  build-windows:
    name: Build (windows-${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
        - arch: x86-64
          runner: windows-latest
          msystem: MINGW64
          prefix: mingw-w64-x86_64
        - arch: arm64
          runner: windows-11-arm
          msystem: CLANGARM64
          prefix: mingw-w64-clang-aarch64

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Set up MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: ${{ matrix.msystem }}
        update: ${{ matrix.arch == 'arm64' }}
        # Disable the install cache: a restored cache carries a stale pacman DB
        # whose signatures the older cached keyring rejects ("unknown trust"),
        # breaking setup after MSYS2 rotates signing keys.
        cache: false

    - name: Install dependencies with mirror retry
      shell: msys2 {0}
      env:
        PREFIX: ${{ matrix.prefix }}
        COMPILER_PACKAGE: ${{ matrix.arch == 'x86-64' && format('{0}-gcc', matrix.prefix) || format('{0}-clang', matrix.prefix) }}
      run: |
        packages=(
          "${PREFIX}-cmake" "${COMPILER_PACKAGE}" "${PREFIX}-ninja"
          "${PREFIX}-pkgconf" "${PREFIX}-sdl3" "${PREFIX}-glm"
          "${PREFIX}-openssl" "${PREFIX}-zlib" "${PREFIX}-ffmpeg"
          "${PREFIX}-unicorn" "${PREFIX}-vulkan-loader"
          "${PREFIX}-vulkan-headers" "${PREFIX}-shaderc"
          "${PREFIX}-libtommath" "${PREFIX}-libtomcrypt" git zip
        )
        for attempt in 1 2 3; do
          if pacman -S --noconfirm --needed "${packages[@]}"; then
            exit 0
          fi
          echo "MSYS2 package download failed on attempt ${attempt}; retrying..."
          sleep $((attempt * 10))
        done
        exit 1

    - name: Build StormLib from source
      shell: msys2 {0}
      run: |
        git clone --depth 1 https://github.com/ladislav-zezula/StormLib.git /tmp/StormLib
        # Disable x86 inline asm in bundled libtomcrypt (bswapl/movl) -
        # __MINGW32__ is defined on CLANGARM64 which incorrectly enables x86 asm
        cmake -S /tmp/StormLib -B /tmp/StormLib/build -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_INSTALL_PREFIX="$MINGW_PREFIX" \
          -DBUILD_SHARED_LIBS=OFF \
          -DCMAKE_C_FLAGS="-DLTC_NO_BSWAP" \
          -DCMAKE_CXX_FLAGS="-DLTC_NO_BSWAP"
        cmake --build /tmp/StormLib/build --parallel $(nproc)
        cmake --install /tmp/StormLib/build

    - name: Configure
      shell: msys2 {0}
      run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

    - name: Verify Release Config
      shell: msys2 {0}
      run: |
        cmake -LA -N build | grep -E '^CMAKE_BUILD_TYPE:STRING=Release$'

    - name: Build
      shell: msys2 {0}
      run: cmake --build build --parallel $(nproc)

    - name: Bundle DLLs
      shell: msys2 {0}
      run: |
        # wowee_assets was missing from this list, so the window's own DLLs
        # were only ever bundled by happening to also be the client's. All
        # three are required: a release with no asset tools is a release
        # nobody can get data for.
        for exe in build/bin/wowee.exe build/bin/asset_extract.exe build/bin/wowee_assets.exe; do
          test -x "$exe"
          ldd "$exe" \
            | awk '/=> \// { print $3 }' \
            | grep -iv '^/c/Windows' \
            | xargs -I{} sh -c 'cp -n "{}" build/bin/ 2>/dev/null || true'
        done

    - name: Package
      shell: msys2 {0}
      run: |
        TAG="${GITHUB_REF_NAME}"
        STAGING="wowee-${TAG}-windows-${{ matrix.arch }}"
        mkdir -p "${STAGING}"

        # Binary and DLLs
        cp build/bin/wowee.exe "${STAGING}/"
        cp build/bin/*.dll "${STAGING}/" 2>/dev/null || true

        # Asset extraction, required rather than copied if it happens to be
        # there. Both of these were behind `if [ -f ]`, so a configure that
        # quietly disabled them - StormLib or SDL2 not found - shipped a
        # release with no way to build assets at all and nothing said so. That
        # is how wowee_assets came to be built for weeks and packaged never.
        test -x build/bin/asset_extract.exe
        cp build/bin/asset_extract.exe "${STAGING}/"

        # The asset manager window, and the scripts for the command line.
        test -x build/bin/wowee_assets.exe
        cp build/bin/wowee_assets.exe "${STAGING}/"
        cp extract_assets.ps1 "${STAGING}/"
        cp extract_assets.bat "${STAGING}/"

        # This client's own addons, beside the exe where the addon search looks.
        cp -r addons "${STAGING}/"

        # Assets (exclude proprietary music)
        mkdir -p "${STAGING}/assets"
        for d in build/bin/assets/*/; do
          dirname="$(basename "$d")"
          [ "$dirname" = "Original Music" ] && continue
          cp -r "$d" "${STAGING}/assets/"
        done
        # Copy top-level asset files
        cp build/bin/assets/* "${STAGING}/assets/" 2>/dev/null || true

        # Data directory (git-tracked files only)
        git ls-files Data/ | while read -r f; do
          mkdir -p "${STAGING}/$(dirname "$f")"
          cp "$f" "${STAGING}/$f"
        done

        # Create ZIP
        zip -r "${STAGING}.zip" "${STAGING}"

    - name: Upload artifact
      uses: actions/upload-artifact@v5
      with:
        name: wowee-windows-${{ matrix.arch }}
        path: wowee-*.zip
        if-no-files-found: error

  build-android:
    name: Build (android arm64)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'

    - name: Install the NDK the project pins
      run: |
        # android/app/build.gradle names this version; the runner ships another.
        yes | "${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager" \
          --install "ndk;28.2.13676358" "platforms;android-34" "build-tools;34.0.0" >/dev/null

    - name: Cache OpenSSL for Android
      id: cache-openssl
      uses: actions/cache@v4
      with:
        path: build-android-deps
        key: android-openssl-${{ hashFiles('tools/build-android-deps.sh') }}

    - name: Build OpenSSL for arm64
      if: steps.cache-openssl.outputs.cache-hit != 'true'
      run: |
        # ANDROID_HOME is set by the runner image, so it is read in the shell
        # rather than through an expression, which only sees workflow env.
        export ANDROID_NDK_ROOT="${ANDROID_HOME}/ndk/28.2.13676358"
        tools/build-android-deps.sh arm64-v8a

    - name: Build the APK
      working-directory: android
      run: |
        export ANDROID_NDK_ROOT="${ANDROID_HOME}/ndk/28.2.13676358"
        ./gradlew :app:assembleRelease --no-daemon

    - name: Name it after the tag
      run: |
        TAG="${GITHUB_REF_NAME}"
        APK=android/app/build/outputs/apk/release/app-release.apk
        # assembleRelease is signed with the debug key: this is a sideloaded
        # build, not a Play listing, and an unsigned APK will not install at all.
        test -f "${APK}" || APK=android/app/build/outputs/apk/release/app-release-unsigned.apk
        cp "${APK}" "wowee-${TAG}-android-arm64.apk"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: wowee-android-arm64
        path: wowee-*-android-arm64.apk

  release:
    name: Create Release
    needs: [ build-linux, build-macos, build-windows, build-android ]
    runs-on: ubuntu-latest

    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v5
      with:
        path: artifacts/

    - name: Create GitHub Release
      env:
        GH_TOKEN: ${{ github.token }}
        GH_REPO: ${{ github.repository }}
      run: |
        TAG="${GITHUB_REF_NAME}"

        # Collect all release files
        FILES=()
        for f in artifacts/*/*; do
          FILES+=("$f")
        done

        if gh release view "${TAG}" >/dev/null 2>&1; then
          gh release upload "${TAG}" --clobber "${FILES[@]}"
        else
          gh release create "${TAG}" \
            --title "Wowee ${TAG}" \
            --generate-notes \
            "${FILES[@]}"
        fi
